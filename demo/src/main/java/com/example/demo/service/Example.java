package com.example.demo.service;

import java.util.Arrays;
import java.util.LinkedHashSet;

public class Example {

	/*
	 * static void removeDuplicate(char character[], int length) { int index = 0;
	 * for (int i = 0; i < length; i++) { int j; for (j = 0; j < i; j++) {
	 * 
	 * if (character[i] == character[j]) { break; }
	 * 
	 * } if (j == i) { character[index++] = character[i]; }
	 * 
	 * } System.out.println(String.valueOf(Arrays.copyOf(character, index))); }
	 */
	public static void main(String[] args) {
		 
		/*
		 * String str = "abcdea"; char characterStr[] = str.toCharArray(); for(int
		 * i=characterStr.length-1;i>=0;i--) { System.out.println(characterStr[i]); }
		 */
		/*
		 * String str = "abcdea"; char characterStr[] = str.toCharArray(); int length =
		 * characterStr.length; LinkedHashSet<Character> linkedHashSet=new
		 * LinkedHashSet<Character>(); for(int i=0;i<length;i++) {
		 * linkedHashSet.add(characterStr[i]); } for(Character character:linkedHashSet)
		 * { System.out.println(character); }
		 */
		
		//removeDuplicate(characterStr, length);

	}

}
